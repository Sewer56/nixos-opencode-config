# Cache file format
```markdown
# Review Cache: {{arg:domain}}
Latest Actions: <actions_path>

## Verified Observations
- <{{arg:ref_type}}>: <grounding snapshot — one line each>

{{ if=with_audit_ledger==1 }}
## Audit Ledger
- <full-artifact pass note or arbitration note>
{{ endif }}
## Finding Ledger
| ID | Status |{{ if=with_implement_cols==1 }} File | Lines |{{ endif }} Category | Severity | Introduced In | Latest Action | Resolution |
|---|---|{{ if=with_implement_cols==1 }}---|---|{{ endif }}---|---|---|---|---|
| {{arg:prefix}}-NNN | OPEN |{{ if=with_implement_cols==1 }} — | — |{{ endif }} — | — | — | — | — |
```