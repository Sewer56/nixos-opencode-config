1. Run `{{arg:reviewer_a}}` and `{{arg:reviewer_b}}` independently {{arg:run_context}}.
2. Do not pass either leg the other leg's output. Do not apply raw leg findings. Do not allow either leg to edit {{arg:no_edit_targets}}.
3. Validate both outputs: `# REVIEW`{{arg:validation_extra}} and `Decision: PASS | ADVISORY | BLOCKING`. Treat `IDs:` as routing data only.
4. Parse findings from each leg's inline `## Findings` section. Do not read sidecar files.
5. Merge findings: {{arg:merge_scope}}.
6. Merge duplicates and resolve conflicting fixes by preferring concrete evidence over reviewer confidence and minimal diffs over broad rewrites.
7. Inspect {{arg:inspect_context}} yourself after reviewer validation. Do not read prior review caches.
8. Emit merged findings inline in the output block. Do not write cache or actions files.
9. GATE: your response MUST contain exactly one `# REVIEW` fenced block with `Decision:`{{arg:validation_extra}}, `## Findings`, and conditional `IDs:`. Do not add `Cache:`, `Actions:`, headings, tables, summaries, narrative prose, or content outside the block. The caller validates this exact shape and rejects malformed responses as protocol failures.