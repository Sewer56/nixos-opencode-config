# Output

```text
# REVIEW
Decision: PASS | BLOCKING | ADVISORY
IDs: F-001, F-002, ...

## Findings
{{
  file="./agent/_templates/review-finding.txt"
  prefix=F
  evidence="{{arg:evidence}}"
  problem="<one line>"
  fix="<smallest concrete correction>"
  file_ref="src/lib.rs"
  bad="-old content"
  good="+new content"
  with_file=1
  with_lines=1
}}

## Notes
- <optional short notes>
```

Return ONLY the block above. Write `- None` under empty sections.

# Constraints
- Drop broad rewrites. Answer whether the implementation is free of blocking issues.
