Enumerate: `export` keyword or `declare` in `.d.ts`. Skip: no `export` (file-scoped).
Module Boundary: directory containing `package.json`.
Re-export: `export * from 'module'`, `export { item } from 'module'`, re-exporting `index.ts`/`index.js`.
Qualified Path: `import { item } from 'module'` or `module.item`.
Test Patterns: `*.test.ts`, `*.spec.ts`, `__tests__/`.
Special Case: `declare namespace` / `declare global` augmentations expose symbols without `export` → enumerate as public.
