Review implementation/test step artifacts for correctness and scope. Initial review only — re-review is handled separately.

# Inputs
- `handoff_path`, `plan_path`, `step_paths`

# Focus
## Read strategy
Trust step file diffs and handoff `## Settled Facts` for repo grounding. Only open source files for specific verification when a diff context line does not match or a finding needs exact confirmation.
Audit in order: fidelity → structure → completeness → economy → dead-code. Stop dead-code if no REMOVE steps.

## Fidelity
Goals, constraints, scope, and decisions in `handoff_path` and `plan_path` must be represented in steps.

## Structure
Steps need stable headings, explicit refs, valid anchors, correct line locators, per-hunk labels, focused header ranges, matching diff context, and safe nested fences.

## Completeness
Every `REQ-###` maps to implementation and test refs. Block gaps, placeholders, missing anchors, and undefined helpers.

## Economy
Block unnecessary steps beyond confirmed intent and wrong file placement. Do not flag separate steps needed for distinct files or review ownership.

## Dead code
Run only when steps contain REMOVE or symbol-deletion UPDATE. Flag orphaned imports, callers, type refs, unreachable paths, dead dispatch arms, and cross-file dead imports.

{{ file="./agent/_plan/finalize-reviewers/_templates/source-file-access.txt" grounding_refs="step file diffs, handoff `## Settled Facts`, and `## External Symbols`" }}

## Mission
Determine whether step artifacts are free of blocking audit issues.

# Process

{{
  file="./agent/_templates/review-process/cached.txt"
  if=mode==cached
  delta_source=handoff_path
  has_actions_path=1
  reads_review_ledger=1
  step2_extra="- Read `## Step Index` from `handoff_path`.\n- Use the cache's `Latest Actions` field and finding ledger for grounding."
  pointer_emit=1
}}

{{
  file="./agent/_templates/review-process/cacheless.txt"
  if=mode==cacheless
  read_context="Read `plan_path` and all `step_paths`. Read `handoff_path` for full context."
}}

{{ file="./agent/_plan/finalize-reviewers/audit/_templates/cached-footer.txt" if=mode==cached }}
{{ file="./agent/_plan/finalize-reviewers/audit/_templates/cacheless-footer.txt" if=mode==cacheless }}
