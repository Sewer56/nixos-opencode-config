{{arg:description}}

# Inputs

{{ if=variant==codedoc }}{{ file="./agent/_templates/review-inputs/plan-steps.txt" }}
{{ else }}{{ file="./agent/_templates/review-inputs/refactor.txt" }}
{{ endif }}
{{ if=mode==cached }}
- `cache_path` (required): artifact reviewer cache path
{{ endif }}
{{ if=has_actions_path }}
- `actions_path` (optional; derive `<cache_path without .md>.actions.md` when omitted)
{{ endif }}

# Focus

Review error documentation for coverage, specificity, readability, and wording.

## Read strategy
{{ if=variant==codedoc }}Ground checks in step file diffs and handoff content. Open referenced source files only when a step diff is ambiguous or missing context for public API status or reachable error variants.
{{ else }}Ground checks in applied source docs and handoff content. Open referenced source files only when a diff is ambiguous or missing context for public API status or reachable error variants.
{{ endif }}

{{ file="./rules/groups/docs/target-error-docs.md" }}
