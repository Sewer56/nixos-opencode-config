{{
  file="./agent/_templates/review-output/output.txt"
  agent="{{arg:agent_name}}"
  prefix={{arg:err_prefix}}
  categories="COVERAGE | SPECIFICITY | FIDELITY"
  evidence="{{arg:evidence1}}"
  problem="<what is wrong>"
  fix="<smallest concrete correction>"
  file_ref="{{arg:file_ref}}"
  bad="-missing or vague error docs"
  good="+concrete # Errors docs"
  with_lines=1
  with_detail=0
  mode={{arg:mode}}
  verified_ref="{{arg:verified_ref}}"
  return_rule="Return ONLY the block above. Write `- None` under empty sections."
}}

# Constraints

- Flag missing `# Errors` sections on public error-returning APIs as BLOCKING per the errors rules.
- Include a unified diff after every finding's `Fix:` field{{arg:diff_target_note}}.

# Rules

{{ file="./rules/docs/errors.md" }}