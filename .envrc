# Use the repository flake when present. The uploaded setup may be deployed
# without its host-specific flake, so keep local tools usable instead of failing.
if [[ -f flake.nix ]]; then
  use flake . --impure
else
  PATH_add "$PWD/bin"
  log_status "flake.nix not present; added ./bin to PATH"
fi
