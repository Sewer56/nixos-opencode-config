---
description: "Migrate opencode-source production onto an upstream version"
agent: migrate
---

# Migrate OpenCode to $ARGUMENTS

Target version: `$ARGUMENTS`.

<output_contract>
Return the exact `# MIGRATION RESULT` block defined by `migrate`.
</output_contract>
