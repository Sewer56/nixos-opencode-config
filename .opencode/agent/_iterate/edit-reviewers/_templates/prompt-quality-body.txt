{{
  file="../config/agent/_templates/review-mission.txt"
  artifact_type="prompt"
  domain="prompt-quality"
}}
{{
  file="../config/agent/_templates/review-process/cached.txt"
  if=mode==cached
  delta_source=log_path
  step2_extra="- Skip workflow pattern catalogs and pattern contracts.\n- If `static_check_path` is provided, read it for changed_paths and mechanical-gate status only.\n- Inspect changed prompt files and directly referenced files needed to detect duplication only.\n- Leave reviewer topology and template feature use to `topology`.\n- Leave render checks, rendered whitespace, markdown fence lint to static script."
  preserve_byte_exact=1
}}
{{
  file="../config/agent/_templates/review-process/cacheless.txt"
  if=mode==cacheless
  read_context="Read `log_path` and directly referenced files from scratch. Ignore prior caches."
  reads_review_ledger=1
}}
{{
  file="../config/agent/_templates/review-cache-table.txt"
  if=mode==cached
  domain=prompt-quality
  ref_type=path
  prefix=PQ
}}
{{
  file="./agent/_iterate/edit-reviewers/_templates/cached-footer.txt"
  if=mode==cached
  agent="_iterate/edit-reviewers/prompt-quality"
  prefix=PQ
  domain=prompt-quality
  categories="RUNTIME_INSTRUCTION | TIGHT_INPUTS | OUTPUT_SCHEMA | WORDING | CAVEMAN | CLARITY | DEDUP"
  evidence="<section, rule, or rendered artifact>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  output_extra="- For CAVEMAN finding: cite violated rule, show bad line, give caveman rewrite."
}}
{{
  file="../config/agent/_templates/review-footer/cacheless.txt"
  if=mode==cacheless
  agent="_iterate/edit-reviewers/prompt-quality"
  prefix=PQ
  with_domains=0
  categories="RUNTIME_INSTRUCTION | TIGHT_INPUTS | OUTPUT_SCHEMA | WORDING | CAVEMAN | CLARITY | DEDUP"
  evidence="<section, rule, or rendered artifact>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  with_verified=1
  verified_ref="<path>: <one-line verified condition>"
  output_extra="- Final-gate cacheless pass: ignore prior caches, return current findings inline only.\n- For CAVEMAN findings on existing grandfathered rule cards, skip; flag NEW or CHANGED rule cards and prompts only."
}}

# Constraints
- BLOCKING:
  - LLM runtime prompt written as docs instead of executable instruction
  - operational behavior lives only in docs
  - callee-owned instructions duplicated in caller
  - unstable machine output
  - confusing behavior-governing text
  - NEW or CHANGED rule card, command body, agent prompt, or reviewer prompt violates caveman-rule.txt
- ADVISORY: local wording economy, positive wording, dense paragraph rules, doc clarity improvement with no correctness effect.
- Keep response compact; detailed evidence belongs in cache (cached mode only).
