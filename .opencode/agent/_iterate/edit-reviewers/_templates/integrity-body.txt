{{
  file="../config/agent/_templates/review-mission.txt"
  artifact_type="prompt"
  domain="integrity"
}}
{{
  file="../config/agent/_templates/review-process/cached.txt"
  if=mode==cached
  delta_source=log_path
  step2_extra="- If `static_check_path` is provided, read it for changed_paths and mechanical-gate status only.\n- Read `config/doc/workflow/optimize-maintenance.md` only when `risk_flags` includes `optimizer-workflow` or changed paths include `config/agent/_workflow/optimize*.md` or `config/agent/_workflow/optimize/export-analyzer.md`.\n- Inspect changed paths plus directly referenced files needed to validate wiring and permissions."
  preserve_byte_exact=1
}}
{{
  file="../config/agent/_templates/review-process/cacheless.txt"
  if=mode==cacheless
  read_context="Read `log_path` and directly referenced files from scratch. Ignore prior caches."
  reads_review_ledger=1
}}
{{
  file="../config/agent/_templates/review-cache-table.txt"
  if=mode==cached
  domain=integrity
  ref_type=path
  prefix=INT
}}
{{
  file="./agent/_iterate/edit-reviewers/_templates/cached-footer.txt"
  if=mode==cached
  agent="_iterate/edit-reviewers/integrity"
  prefix=INT
  domain=integrity
  categories="FRONTMATTER | WIRING | PERMISSION | SCOPE | SELF_ITERATION | OPTIMIZER | SOURCE_BOUNDARY"
  evidence="<line or field showing issue>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
}}
{{
  file="../config/agent/_templates/review-footer/cacheless.txt"
  if=mode==cacheless
  agent="_iterate/edit-reviewers/integrity"
  prefix=INT
  with_domains=0
  categories="FRONTMATTER | WIRING | PERMISSION | SCOPE | SELF_ITERATION | OPTIMIZER | SOURCE_BOUNDARY"
  evidence="<line or field showing issue>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  with_verified=1
  verified_ref="<path>: <one-line verified condition>"
  output_extra="- Final-gate cacheless pass: ignore prior caches, return current findings inline only."
}}

# Constraints
- BLOCKING: broken command/agent wiring, unsafe permissions, invalid frontmatter semantics, missing model-facing self-iteration rule, optimizer architecture regression, source-boundary violation, target-scope violation.
- ADVISORY: harmless doc mismatch, minor convention drift, maintainability cleanup with no behavior change.
- Keep response compact; detailed evidence belongs in cache (cached mode only).
