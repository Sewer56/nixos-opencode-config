<actions_schema>
Write actions file:
```markdown
# Review Actions: {{arg:domain}}
Source Cache: [[cache_path]]

## Findings
{{
  file="../config/agent/_templates/review-finding.txt"
  prefix={{arg:prefix}}
  categories="{{arg:categories}}"
  evidence="{{arg:evidence}}"
  problem="{{arg:problem}}"
  fix="{{arg:fix}}"
  file_ref="{{arg:file_ref}}"
  bad="{{arg:bad}}"
  good="{{arg:good}}"
  with_file={{arg:with_file}}
  with_lines={{arg:with_lines}}
  with_evidence={{arg:with_evidence}}
  with_detail={{arg:with_detail}}
  detail="{{arg:detail}}"
  with_step_file={{arg:with_step_file}}
  step="{{arg:step}}"
}}
```
</actions_schema>

<output_contract>
Return ONLY fenced block:
```text
# REVIEW
Cache: [[cache_path]]
Actions: [[actions_path]]
Agent: {{arg:agent}}
Decision: PASS | ADVISORY | BLOCKING
IDs: {{arg:prefix}}-001, {{arg:prefix}}-002, ...

## Findings
- See `actions_path` for OPEN findings.
- Use `- None` when no current findings.

## Verified
- See `cache_path` for verified observations.
- Use `- None` when none.

## Notes
- [[optional_short_note]]
```
</output_contract>

<constraints>
PASS omits `IDs`. Every response ID must exist in cache and actions. Response is pointer-only; details go to actions, evidence to cache.
{{ if=output_extra }}
{{arg:output_extra}}
{{ endif }}
</constraints>
