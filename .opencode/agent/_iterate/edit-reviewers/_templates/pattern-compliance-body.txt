{{
  file="../config/agent/_templates/review-mission.txt"
  artifact_type="prompt"
  domain="pattern-compliance"
}}
{{
  file="../config/agent/_templates/review-process/cached.txt"
  if=mode==cached
  delta_source=log_path
  step2_extra="- Read `pattern_contract_path`.\n- Read selected source sections named by contract, such as `config/doc/workflow/design-patterns.md#OPT-###` or `config/doc/workflow/optimize-patterns.md#WOPT-###`.\n- Read changed files and any `Apply To` files from contract.\n- If `static_check_path` is provided, read it only when selected Validation bullet depends on static/render status.\n- Check each selected Carry-In, Quality Guard, Apply To path, Validation bullet against generated prompt text.\n- Findings target generated files that miss selected patterns."
  preserve_byte_exact=1
}}
{{
  file="../config/agent/_templates/review-process/cacheless.txt"
  if=mode==cacheless
  read_context="Read `log_path` and `pattern_contract_path` from scratch. Ignore prior caches."
  reads_review_ledger=1
}}
{{
  file="../config/agent/_templates/review-cache-table.txt"
  if=mode==cached
  domain=pattern-compliance
  ref_type=pattern-or-path
  prefix=PAT
}}
{{
  file="./agent/_iterate/edit-reviewers/_templates/cached-footer.txt"
  if=mode==cached
  agent="_iterate/edit-reviewers/pattern-compliance"
  prefix=PAT
  domain=pattern-compliance
  categories="CARRY_IN | QUALITY_GUARD | APPLY_TO | VALIDATION"
  evidence="<pattern id or missing element>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  with_verified=1
  verified_ref="<pattern id or path>: <one-line verification>"
}}
{{
  file="../config/agent/_templates/review-footer/cacheless.txt"
  if=mode==cacheless
  agent="_iterate/edit-reviewers/pattern-compliance"
  prefix=PAT
  with_domains=0
  categories="CARRY_IN | QUALITY_GUARD | APPLY_TO | VALIDATION"
  evidence="<pattern id or missing element>"
  problem="<one-line problem>"
  fix="<exact correction>"
  file_ref="<repo-relative path>"
  bad="-<wrong line>"
  good="+<correct line>"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  with_verified=1
  verified_ref="<pattern id or path>: <one-line verification>"
  output_extra="- Final-gate cacheless pass: ignore prior caches, return current findings inline only."
}}

# Constraints
- BLOCKING: selected Carry-In, Quality Guard, or Validation bullet missing or contradicted in generated prompt text.
- ADVISORY: selected behavior present but weak or indirect.
- Keep `opencode-source/` unread and unused.
