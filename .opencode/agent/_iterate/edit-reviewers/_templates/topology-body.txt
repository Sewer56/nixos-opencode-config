{{
  file="../config/agent/_templates/review-mission.txt"
  artifact_type="prompt"
  domain="topology"
}}
{{
  file="../config/agent/_templates/review-process/cached.txt"
  if=mode==cached
  delta_source=log_path
  step2_extra="- Inspect changed prompts plus referenced files needed to judge split/merge/script/template use.\n- Check concern count, context cost, handoff size, and reviewer coverage.\n- Leave render/whitespace lint to static script."
  preserve_byte_exact=1
}}
{{
  file="../config/agent/_templates/review-process/cacheless.txt"
  if=mode==cacheless
  read_context="Read `log_path` and directly referenced files from scratch. Ignore prior caches."
  reads_review_ledger=1
}}
{{
  file="../config/agent/_templates/review-cache-table.txt"
  if=mode==cached
  domain=topology
  ref_type=path
  prefix=TOP
}}
{{
  file="./agent/_iterate/edit-reviewers/_templates/cached-footer.txt"
  if=mode==cached
  agent="_iterate/edit-reviewers/topology"
  prefix=TOP
  domain=topology
  categories="SPLIT | MERGE | SCRIPT | TEMPLATE | PARALLELISM | HANDOFF | CONTEXT"
  evidence="[[section_rule_or_rendered_artifact]]"
  problem="[[one_line_problem]]"
  fix="[[exact_correction]]"
  file_ref="[[repo_relative_path]]"
  bad="-[[wrong_line]]"
  good="+[[correct_line]]"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
}}
{{
  file="../config/agent/_templates/review-footer/cacheless.txt"
  if=mode==cacheless
  agent="_iterate/edit-reviewers/topology"
  prefix=TOP
  with_domains=0
  categories="SPLIT | MERGE | SCRIPT | TEMPLATE | PARALLELISM | HANDOFF | CONTEXT"
  evidence="[[section_rule_or_rendered_artifact]]"
  problem="[[one_line_problem]]"
  fix="[[exact_correction]]"
  file_ref="[[repo_relative_path]]"
  bad="-[[wrong_line]]"
  good="+[[correct_line]]"
  with_file=1
  with_lines=1
  with_evidence=1
  with_detail=0
  with_step_file=0
  detail=""
  step=""
  with_verified=1
  verified_ref="[[path]]: [[verified_condition]]"
  output_extra="- Cacheless final gate: ignore caches; return current findings inline only."
}}

<review_scope>
BLOCKING: split adds prompt/read cost without lowering concern/context; deterministic phase kept as subagent; high-risk ownership lost; cacheless reviewer uses sidecar; one-consumer rule/template extracted instead of inlined; independent reviewers serialized without dependency; broad investigation stays in primary context. ADVISORY: minor template/path convention drift.
</review_scope>
