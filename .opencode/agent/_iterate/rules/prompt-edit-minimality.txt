- Apply caveman rule at `.opencode/agent/_iterate/rules/caveman-rule.txt` to every NEW or CHANGED rule card, command body, agent prompt, reviewer prompt.
- Match frontmatter permissions to tools prompt body uses.
- Remove unavailable-tool instructions.
- Remove unused tool permissions.
- Remove denials already enforced by frontmatter.
- Pass subagents only run paths, ids, flags, notes, decisions, cache/action paths.
- Let callee own role, scope, process, examples, output schema.
- Use positive action wording.
- Define derived path once; reuse variable.
- Remove filler, duplicate rules, trailing spaces, whitespace-only lines, repeated blank lines.
- Keep selected OPT/WOPT carry-ins only when they change target prompt.
- Use imperative concrete verbs.
- Number sequential workflow phases only.
- Prefer bullets/checklists for runtime rules.
